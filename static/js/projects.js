document.addEventListener("DOMContentLoaded", function () {
    const downloadButton = document.querySelector(".explore-btn");

    downloadButton.addEventListener("click", function (event) {
        // Xabar chiqarishdan saqlanish
        // event.preventDefault(); // Agar kerak bo'lsa, ushbu qatorni faollashtiring
        // Agar xabar chiqarishga hojat yo'q bo'lsa, yuqoridagi qatorni olib tashlang
    });
});
